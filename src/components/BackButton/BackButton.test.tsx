import { render } from '@testing-library/react'
import BackButton from './BackButton'

describe('Card component', () => {
  test('should correctly renders', () => {
    render(<BackButton />)
  })
})
